{
  "name": "z-browser-ui",
  "version": "1.0.0",
  "description": "Z-Browser with UI controls (address bar + navigation)",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^29.0.0"
  }
}